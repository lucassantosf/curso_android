<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="qualserie.cursoandroid.com.qualserie.MainActivity">

    <TextView
        android:text="@string/pouco"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/seekBarId"
        android:layout_toLeftOf="@+id/seekBarId"
        android:layout_toStartOf="@+id/seekBarId"
        android:layout_marginBottom="27dp"
        android:id="@+id/PoucoId"
        android:textSize="18sp" />

    <TextView
        android:text="@string/muito"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/PoucoId"
        android:layout_toRightOf="@+id/seekBarId"
        android:layout_toEndOf="@+id/seekBarId"
        android:id="@+id/MuitoId"
        android:textSize="18sp" />

    <ImageView
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/imagemExibicaoId"
        android:layout_width="170dp"
        android:background="@drawable/pouco"
        android:layout_height="170dp" />

    <ImageView
        android:id="@+id/imageView5"
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:contentDescription=""
        android:background="@drawable/logo"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <SeekBar
        android:layout_height="wrap_content"
        android:layout_marginBottom="49dp"
        android:id="@+id/seekBarId"
        android:max="4"
        android:layout_above="@+id/imagemExibicaoId"
        android:layout_centerHorizontal="true"
        android:layout_width="170dp" />

</RelativeLayout>
